{
  "version": 2,
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "(.*)",
      "dest": "src/server.ts"
    }
  ],
  "env": {
    "DATABASE_URL": "postgresql://ordem%20de%20servi%C3%A7o_owner:ONT9fJS6ELVc@ep-old-voice-a8ddizjr.eastus2.azure.neon.tech/ordem%20de%20servi%C3%A7o?sslmode=require",
    "NODE_ENV": "production"
  },
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npm run build",
  "installCommand": "npm install",
  "framework": null,
  "scripts": {
    "vercel-build": "npx prisma migrate deploy"
  }
} 